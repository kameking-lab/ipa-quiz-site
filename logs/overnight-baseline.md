# 夜間自律改善 自己採点ベースライン

測定: 2026-05-30 (土) 早朝 JST / 凍結点 main = `ea2ca69` / タグ `pre-overnight-20260530`
朝（2026-05-31 09:00 JST 以降）、Chrome agent はこの基準で overnight-integration（プレビュー or localhost 本番ビルド）を
再採点する。**ベースラインを上回ったら main へマージ可。下回る/同等なら main 不変のまま個別 revert。**

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## A. CLI 健全性ベースライン（凍結点 main で実測）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- `pnpm typecheck`: **PASS（エラー 0）** ← しきい値: 0 errors（必達）
- `pnpm lint`: **PASS（エラー 0 / 警告 1）** ← しきい値: errors 0。警告は 1 以下（後述の既知 1 件のみ許容、増やさない）
  - 既知の 1 警告: `scripts/ux-audit-screenshots.mjs:88` `'alteredFocusVisible' is assigned but never used`
    ※これは**未追跡ファイル**（前セッションの作業ツリー残置物）由来。overnight-integration には含めない。
- `pnpm test`: **PASS（32 ファイル / 205 テスト 全緑）** ← しきい値: 32 ファイル・205 テスト以上が緑（減らさない）
- `pnpm build`: **PASS（本番ビルド成功）** ← しきい値: 成功（必達）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## B. 本番（frozen main）実測ベースライン — curl 実値
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
測定対象: 本番 `https://www.kakomon-ai.jp`（= 凍結された main のデプロイ）
- ホーム `/`: **HTTP 200** ← しきい値: 200
- `/admin`: **HTTP 401** ← しきい値: 401（503 は退行とみなす）。**既に 401。P0-① は本番で解決済みの可能性大。**
- `/ip`（IP 試験 LP）: 問題数表示 **2,381** ← しきい値: 2,381（SSOT 一致。2,398 等の混入は退行）
- `/blog/ip-nani-kara-benkyou`: 問題数表示 **2,381** ← しきい値: 2,381。**既に 2,398 は消滅（再デプロイ済）。P0-② は本番で解決済み。**

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## C. P0 既知項目の現状（夜間ループの着手判断材料）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- **P0-① /admin 503→401**: 本番で既に **401**。→ コード側も 401 が正なら **done**（要 read-only 確認のみ）。
  503 が再現しないなら触らず done 記録。env/KV 起因の 503 は SKIP。
- **P0-② blog 2,398→2,381**: 本番で既に **2,381**、ソース grep でも 2,398 はソース不在（logs/test のみ）。
  → これは **デプロイ stale 起因の過去事象**。コードで直す対象なし。**SKIP（実害なし・解決済み）**。
- **P0-③ バッジトースト 5 秒自動消滅**: 位置は上部へ移動済み（c94339e）。自動消滅の挙動を E2E で要実証。
  → 実害（消えずに残る）が再現したら修正。再現しなければ done 記録。
- **P0-④ Q&A dateCreated 任意警告 11 件**: `lib/seo/question-jsonld.ts` の dateCreated が TZ 付き ISO か要確認。
  → TZ 無し/日付のみなら TZ 付き ISO 化（+09:00）。実測で JSON-LD を確認してからコミット。

> つまり夜間ループの実質的な真の作業は **P0-③/④ の検証＋（必要なら）修正と、P1 スイープ**が中心になる見込み。
> P0-①/② に時間を溶かさないこと（過大修正の罠）。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## D. 朝の合否ルール（要約）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
マージ可（main へ反映）の条件 = 次をすべて満たす:
1. overnight-integration で typecheck/lint(err0)/test(205+ 緑)/build がすべて緑。
2. 上記 B の本番実測しきい値を**下回らない**（200 / 401 / 2,381 / 2,381 を維持し退行ゼロ）。
3. 追加された各コミットが「実害ある改善」で、検証（E2E or 実測 curl/grep）が添えられている。
不可の場合 = main 不変のまま、退行を起こしたコミットを `git revert` で個別除外して再採点。
