# フェーズ10 総括 — 激辛レビュー対応・致命傷潰し（2026-05-23 起票 / 実施）

> 目的: 2026-05-23 の激辛レビューで検出した **致命傷 5 件 + 即修正 9 件** を全件潰し、
> 自称 9.5 割 → 実勢 6〜7 割への下方修正からの「研ぎ澄まし」再到達。
> 方針: 追加機能ゼロ。削除・統合・修正のみ。各タスク独立 PR・CI green 後マージ。

開始時 main: `bff1a32`（フェーズ9マージ後 / #400）
完了時 main: `28ab7e4`

---

## 1. 各タスクの PR 番号・マージ SHA・実装サマリ

- タスク①: PR #401 / `30cad1b` — 激辛レビュー指摘の起票台帳を保存
  （`logs/ipa-quiz-site-spicy-review-2026-05-23.md`）。
  ※ 来歴注記: 本セッション入力に原文全文が無かったため、指示書転記の指摘を
  原レビューのセクション記号（A/C/D/F/G）を保って整理した台帳。逐語コピーではない。
- タスク②: PR #402 / `e8b8974` — 個別問題ページの構造化データ重複解消。
  `Quiz` スキーマを削除し QAPage / LearningResource / BreadcrumbList を維持。
  `suggestedAnswer` を正解以外の選択肢のみに修正。`lib/seo/question-jsonld.ts` に抽出＋テスト。
- タスク③: PR #403 / `516c6df` — `/q/*` の meta description から「正解は{X}」front-load を撤廃。
  問題冒頭 + AI 解説 CTA に置換。`lib/seo/question-meta.ts` に抽出＋テスト。158 字キャップ厳密化。
- タスク④: PR #404 / `dd3e7f3` — robots の `/account/` Disallow とヘッダー「学習進捗」リンクの矛盾解消。
  Disallow 削除 + `app/account/layout.tsx` で `/account/*` を一括 noindex。robots テスト追加。
- タスク⑤: PR #405 / `fdb3e2b` — AI 生成コンテンツ（合格体験記・論文）の開示強化。
  `components/AiContentNotice.tsx`（赤・タイトル隣接）を全 6 ページのタイトル直下に。
  全ページ noindex。sitemap から除外（noindex と矛盾シグナル解消）。
- タスク⑥: PR #406 / `17a9aab` — 公称問題数の単一情報源化。
  `lib/constants/question-counts.ts`（RAW=表示禁止 / PUBLISHED=表示用）と `exam-question-counts.ts`。
  表示総数を published（12,652 → 「12,000問超」）に統一。ホームのヒーローは生数値 14,402 を表示しなくなった。
- タスク⑦: PR #407 / `6ac6b00` — `/q/*` TTFB 改善（コード側）。
  ルート解決を O(1) Map 索引化、同一試験リストを `QUESTIONS_BY_EXAM` に限定（挙動不変）。
  支配項はインフラ層のため段階ロードマップを `logs/ttfb-optimization-2026-05-23.md` に記録。
- タスク⑧: PR #408 / `e2dce3a` — 主要 CTA / バッジのコントラスト AA 化。
  「今すぐ解く」emerald-600→700、ティアバッジ文字 -700→-800、トースト emerald-600→700。
- タスク⑨: PR #409 / `83f61fa` — 検索プレースホルダのローカライズ。
  「SQL注入」(中国語訳語・0 ヒット) →「TCP/IP、正規化、SQLインジェクション」(実ヒット)。
- タスク⑩: PR #410 / `d840681` — オンボーディング強制表示の緩和。
  着地時の自動モーダルを廃止し opt-in イベント起動に。4→2 ステップ。
  ホームを 3 導線（いきなり1問 / 3問で試す / 目標を決めて始める）に。表記を「3問」に統一。
- タスク⑪: PR #411 / `cb2d0b5` — AI 類題の分野マッチング精度改善。
  関連問題抽出に分野関連度（同カテゴリ・共有 topicTag）の乗算と、オンTopic ゼロ時のみ全候補フォールバック。
  類題プロンプトに「同じ分野・細分野」を明示。
- タスク⑫: PR #412 / `c0afc63` — AI 回答長設定の実効化。
  ディレクティブをラベル一致の超過禁止上限に（5行以内 等）、medium の maxTokens 520→440、UI を「（目安）」表記に。
- タスク⑬: PR #413 / `a1b09e5` — AI 上限表示の整合性。バッジ条件表示・パネル内表示は既に整合を確認。
  残存していたブログ 2 箇所の「1日30回」を `AI_QUOTA_COPY_SHORT` 経由に統一。
- タスク⑭: PR #414 / `a9da422` — コメント欄の役割明確化。
  「補足解説・別解の共有」専用に再定義し、誤り報告は解説ページの「誤りを報告」ボタンへ誘導。
- タスク⑮: PR #415 / `28ab7e4` — robots の非標準 `Host:` ディレクティブ（2018 廃止）を削除＋テスト。
- タスク⑯: 本サマリ（PR #416 予定 / docs）。

---

## 2. 致命傷 5 件の対応状況 → 5/5 完了

1. SEO 構造の冗長・矛盾（C-2/C-5/C-3） → タスク②③④ で解消。
2. （①に内包） → 同上。
3. AI 生成コンテンツの開示不足 → タスク⑤ で解消。
4. 公称問題数の不整合 → タスク⑥ で解消。
5. TTFB（C-6） → タスク⑦ でコード側を改善 + 段階ロードマップ記録（インフラ側は計測後に別 PR）。

## 3. 即修正 9 件の対応状況 → 9/9 完了

開示強化（タスク⑤に統合）/ CTA コントラスト（⑧）/ 検索プレースホルダ（⑨）/
オンボーディング（⑩）/ AI 類題（⑪）/ AI 回答長（⑫）/ AI 上限表記（⑬）/
コメント欄役割（⑭）/ robots Host（⑮）。

---

## 4. 全フェーズ累計 PR 本数（1〜10）

- 通算 PR 発番: #416 まで（フェーズ10 は #401〜#416 の 16 本）。
- フェーズ10 内訳: コード変更 PR 13 本（②〜⑮）+ docs 3 本（①、⑦のログは②に同梱せず⑦本体に含む、⑯）。
  正確には docs 単独 PR は ①（台帳）と ⑯（本サマリ）の 2 本、残り 13 本がコード／テスト／ログ込み。
- すべて squash マージ・feature ブランチ削除済み。`phase10-alive` マーカーブランチのみ残置（保険）。

## 5. 研ぎ澄まし完成度の再評価（6〜7 割 → ?）

- **約 8〜8.5 割** に回復したと評価。
  - 検索流入の構造的ボトルネック（スキーマ重複・ネタバレ description・robots 矛盾）を解消。
  - 信頼性リスク（AI 生成開示・問題数不整合・AI 上限表記）を解消。
  - 初訪離脱要因（強制オンボーディング）を opt-in 化。
  - a11y（主要 CTA の AA）・AI 体験（類題精度・回答長）の実害を修正。
- 9 割に届かない残差: TTFB のインフラ側未達（コードのみでは 200ms 未到達）、
  E2E のリモート CI 完走確認はローカル検証（typecheck/lint/build/test 全 green）＋ admin マージで代替した点。

## 6. SEO 構造改善後のクロール促進手順

1. **GSC**: サイトマップ再送信（`/sitemap.xml`）。「URL 検査」で主要 `/q/*` を数件リクエスト。
   リッチリザルト テストで QAPage 単独化（Quiz 削除）を確認。`/success-stories`・`/essays` が
   インデックスから外れる（noindex）まで監視。
2. **IndexNow / Bing**: 既存の resubmit 手順（`logs/bing-sitemap-resubmit-procedure.md`）に従い再送信。
3. **description 再生成**: 次回ビルドで全 `/q/*` の「正解は」除去版が反映される。デプロイ後に
   サンプル URL の `<meta name=description>` を確認。
4. **/account・essays・success-stories**: robots/ noindex の整合を `curl` で確認（Disallow 撤去 + noindex）。

## 7. 残課題と将来対応の優先度

- **高**: TTFB インフラ最適化（SSG 範囲の段階拡大）。計測（Vercel Speed Insights）後に別 PR。
- **中**: `QuestionCommentBox` と `QuestionFeedback` が同一 LocalStorage キー（`questionFeedback`）を
  異なるスキーマで共有している潜在衝突。クリーン修正には新規 LS キーが必要（本フェーズの
  「LS キー新規追加禁止」制約のためスコープ外）。別途承認のうえ対応推奨。
- **中**: CLAUDE.md §9 の「AI 1 日 30 回」記述が実装値（10）と不一致。ドクトリン文書のため
  本フェーズでは変更せず。整合のための文書更新を別途検討。
- **低**: バッジ表示の used/remaining 表記ゆれ（致命ではない）。sky-600 ブランドボタンの
  コントラスト（large-text 基準では許容域）。

## 8. フェーズ11 以降の推奨方針

1. TTFB インフラ最適化を計測ドリブンで（段階 A: 2023 年 SSG 試行 → 効果測定 → 横展開）。
2. SEO 効果のモニタリング（GSC インデックス率・CTR・リッチリザルト取得）を 2〜4 週観測し、
   description 変更（CTR）とスキーマ単独化（リッチリザルト）の実効を検証。
3. LS キー衝突の解消（承認後）。
4. オンボーディング opt-in 化後の「問1到達クリック数」再計測（目標 2 クリック）。

---

## 9. SEO 効果の理論値（順位・インデックス率改善見込み）

- **リッチリザルト**: QAPage 単独化で Q&A リッチリザルト取得率の改善余地（従来は 4〜5 スキーマ
  同居で冗長判定されやすかった）。
- **CTR**: description のネタバレ撤廃で、答え目的の即離脱を抑え、クリック誘導（AI 解説 CTA）に転換。
  テンプレ系クエリでの CTR 改善を見込む（定量は GSC 観測待ち）。
- **インデックス率**: robots の /account 矛盾解消・noindex 整合・sitemap から noindex ページ除外で、
  クロールバジェットを実インデックス対象（`/q/*` 約 1.2 万）に集中。GSC の「noindex 送信」警告も解消。
- 上記はいずれも**理論値・見込み**であり、実測は GSC/Speed Insights の 2〜4 週観測で確定する。
