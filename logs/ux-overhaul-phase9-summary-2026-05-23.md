# UX Overhaul Phase 9 — 研ぎ澄まし削減 実装サマリ (2026-05-23)

社長指示「二度手間・くだらない機能はいらない」「研ぎ澄ます」に基づき、**追加機能ゼロ**で
削除 5・統合 3・磨き込み 5 を完走した。フェーズ9は「足し算」から「引き算」への転換点。
正味の差分は **29 ファイル / +578 / −1594 行**(純減 約 1,000 行)で、削除主体であることを
数字が示している。

## 0. 来歴・進め方の注記(重要)

- **タスク①レビューレポート**: Chrome agent の生レポート逐次コピー原本は本タスクに
  添付されていなかったため、フェーズ9ブリーフ内に転記されていた検出項目
  (A-1〜F-3) を一次情報として改変せず再構成した。詳細は
  `logs/ux-seo-sharpening-review-2026-05-23.md` 冒頭の来歴メモ参照。
- **PR/マージ運用の適応**: 本セッションのハーネスは作業ブランチ
  `claude/pensive-lovelace-ICdzm` を割り当て、ドラフト PR 作成を前提としている。
  また CI は重量級の Playwright e2e であり、14 本を順次「CI green 待ち→self-merge」
  するのは現実的でなく、main への不可逆マージ 14 回はハーネス設定とも整合しない。
  そこで **14 タスクを 1 ブランチ上の独立コミット 14 本**として実装し、
  **1 本のドラフト PR** にまとめた。タスク単位の追跡性はコミット粒度で担保し、
  main への最終マージは社長判断に委ねる(必要なら PR 分割も可能)。
  したがって以下「マージ SHA」はコミット SHA を指す。

## 1. タスク別 結果(コミット SHA + 実装内容)

- **タスク① 研ぎ澄ましレビュー保存**: `7bac529`
  — `logs/ux-seo-sharpening-review-2026-05-23.md`(エグゼクティブサマリ / A 二度手間7件 /
  B UX摩擦 / C SEO / D 削除候補 / E 競合比較 / F 即実行TOP / 優先度マトリクス /
  削減ロードマップ / 結論)。

- **タスク② フローティング報告ボタン削除**: `f2dece5`
  — `/q/*` 限定表示の `FeedbackButton`(`fixed bottom-20 right-4 z-40`)を削除。
  孤立した `FeedbackModal` も削除。`DeferredLayoutWidgets` のマウントと dynamic import を撤去。
  z-index/safe-area 干渉を解消。インライン「誤りを報告」は維持。

- **タスク③ ショート動画生成削除**: `b780427`
  — `QuestionVideoButton` と `lib/motivation/video.ts`(Canvas+MediaRecorder パイプライン)を削除。
  ネイティブ API のみ使用のため**依存パッケージ削除なし(pnpm-lock 変更なし)**。
  シェアボタン(URL/X/LINE)は維持。

- **タスク④ FAQPage / EducationalOrganization schema 削除**: `8be5652`
  — 個別問題ページ JSON-LD から FAQPage と EducationalOrganization を削除。
  孤立した `faqEntries` / `getCategoryTip` / `SITE_LOGO_IMAGE` も除去。
  **残った構造化データ: QAPage / Quiz / Question / LearningResource / BreadcrumbList**。
  構造化データ件数を検証するテストは存在せず、更新対象なし。

- **タスク⑤ 模試導線の重複統合**: `c697005`
  — ヘッダードロップダウン「問題を解く」内の模試項目(`QUIZ_MODES`)を削除。
  **模試導線の最終配置(2 箇所)**: ヘッダートップレベルリンク + モバイル底タブ。
  ドロップダウン残項目: 通常クイズ / ストリームモード / 復習モード / 弱点克服 / 論述例(SC)。

- **タスク⑥ AI 上限バッジの条件表示化**: `ca3da3e`
  — **表示条件: AI コパイロットパネル展開中のみ**。常時固定表示を撤去。
  コパイロット開閉を `lib/copilot/visibility`(購読可能な軽量シングルトン)で橋渡しし、
  `AiQuotaIndicator` が `useSyncExternalStore` で購読。回数取得ロジックは不変。
  **LocalStorage キー新規追加なし**。PR #364 の中立表記は維持。

- **タスク⑦ /quickstart のホーム統合 + 301**: `315f350`
  — **リダイレクト設定**: `next.config.ts` に `/quickstart → /` と `/quickstart/:exam → /`(共に 301)。
  `app/quickstart/*`(4 ファイル)削除。**ホームに吸収した要素**: 初訪問者限定の単一 CTA
  「まずは応用情報を3問だけ試す→」(`HomeQuickTrialCta`、`/quiz?mode=random&exam=ap&limit=3`)。
  オンボーディングツアー・初学者学習パスを limit=3 クイズへ再ポイント、sitemap から quickstart 除去。
  「学習スタイル別の推奨ルート」は削除推奨に従い撤去。

- **タスク⑧ フッター報告リンク削除(報告 UI 3→1)**: `0876d45`
  — **フッター変更内容**: 「誤情報を報告」リンクを削除。隣接「フィードバック」(→ /contact)は維持。
  これで報告 UI は解説直下のインライン「誤りを報告」1 箇所に統一。/contact・/api 報告経路は不変。

- **タスク⑨ ホーム「分野で探す」デフォルト折りたたみ**: `20c0e4d`
  — **折りたたみ実装方式**: `HomeTopicGrid` を `<h2>` 内ボタン + `aria-expanded`/`aria-controls` の
  アコーディオン化、デフォルト閉。セッション内のみ保持(永続化なし)。7 分野カードは
  `hidden` 属性で DOM に残置(条件レンダしない)→ 横断リンクはクローラ可読。

- **タスク⑩ AI 注釈の圧縮**: `a9fa4c8`
  — **実装方式**: 3 層注釈を単一「AI生成」バッジ + ポップオーバーに圧縮。ネイティブ `<details>`
  ディスクロージャ(新規依存なし・キーボード/タップ対応・内部リンクはフォーカス可能)。
  ポップオーバー内容: 誤り可能性 / IPA 公式確認 / 最終更新日 / 出典 PDF / 公式解答 PDF / 透明性リンク。

- **タスク⑪ 履歴ありユーザー向けヒーロー差し替え**: `e831e5e`
  — **差し替え条件**: visitCount≥1 かつ lastQuestion 有り。`HomeReturningHeader` の続きカードを
  プロミネントな「続きから解く」ヒーロー(h1 + 最終問題 + 大 CTA + 正答率 + ストリーク)に昇格。
  新規 `HomeHeroLede` が初回ヒーローを返却ユーザーに対し非表示化。SSR/初回描画は初回想定
  レイアウト(h1 を静的 HTML に維持) → ハイドレーション後切替で CLS 最小化。

- **タスク⑫ 個別問題ページ ファーストビュー整理**: `836ca18`
  — 上部シェアを 36px アイコン(WCAG 2.5.8 AA の 24px 下限を上回る)へ縮小。
  パンくず/ヘッダー/バッジ行/難易度/問題文パディング/選択肢間隔/正解リビール余白を圧縮し
  「正解を表示」を上方へ寄せた。**ファーストビュー高さ**: 設問長依存のため厳密な 800px 収まりは
  ブラウザ非搭載環境で画素検証未実施(余白削減は typecheck/build で検証)。

- **タスク⑬ 学習ガイド関連記事 2→4**: `4096397`
  — **関連記事件数: 2 → 4**。`getRelatedBlogPosts` に任意 `fieldTags` を追加し、試験区分一致の
  うち category+topicTags に重なるタグを持つ記事を上位化(related-questions.ts のタグ重なり
  ロジックを応用)。マッチ不足時は自動で件数フォールバック。essays 系既存呼び出しは 2 引数のまま不変。

- **タスク⑭ 関連問題セクションの SSR 化**: `23e4982`
  — **SSR 化方式**: 調査の結果、関連/他年度/他試験セクションは既に Server Component で算出され
  プリレンダ HTML に出力済み(ビルド済み問題ページに `/q/` 内部リンク 14 本を確認)。
  クライアント境界なしのため SSR 変換不要。将来クライアント島へ移さないための不変条件コメントを追加。
  **ビルド時間影響**: generateStaticParams 追加なしで不変。

- (付随) **未使用 import 整理**: `1f686df` — タスク⑦の quickstart リンク削除に伴う `next/link` 未使用解消。

- **タスク⑮ 本サマリ**: 本ドキュメント(コミット後に SHA 確定)。

## 2. レビュー検出への対応状況

- **削除 5/5**: ②フローティング報告 / ③ショート動画 / ④FAQPage+EduOrg schema /
  ⑤模試重複 / ⑥AI上限バッジ常時表示。
- **統合 3/3**: ⑦quickstart→ホーム+301 / ⑧報告UI 3→1(フッター) / ⑨分野アコーディオン。
- **磨き込み 5/5**: ⑩AI注釈圧縮 / ⑪履歴ヒーロー差替 / ⑫FV整理 / ⑬関連記事2→4 / ⑭関連SSR確認。
- セクション A「二度手間 7 件」はすべて対応(報告3重・模試3重・quickstart重複・注釈3層・
  カード過多・上限バッジ常時・関連/シェア冗長の運用ルール化)。

## 3. 削除済み機能の完全リスト

- フローティング「報告」ボタン(`FeedbackButton`)+ `FeedbackModal`
- ショート動画生成(`QuestionVideoButton` + `lib/motivation/video.ts`)
- 個別問題ページの FAQPage 構造化データ
- 個別問題ページの EducationalOrganization 構造化データ
- ヘッダードロップダウン内の「模試」重複項目
- AI 上限バッジの常時固定表示(機能自体は条件表示で存続)
- `/quickstart`・`/quickstart/[exam]` ページ群(301 で / へ)
- /quickstart の「学習スタイル別の推奨ルート」属性ピッカー
- フッター「誤情報を報告」リンク(報告経路は存続)
- AI 解説直下の 3 層注釈(単一バッジ+ポップオーバーへ統合)

## 4. 品質ゲート

- `pnpm typecheck`: green(全タスクで逐次確認)
- `pnpm lint`: green / **警告 0**
- `pnpm test`: green(84/84、退行なし)
- `pnpm build`: green(全ルート プリレンダ成功、/quickstart はルート木から消滅)
- プリレンダ HTML 抜き取り検証: ホームに初回リード/3問CTA/分野アコーディオン(カードは DOM 残置)、
  問題ページに「AI生成」バッジ・FAQPage=0・EduOrg=0・ショート動画=0・QAPage 維持・浮動報告=0 を確認
- 新規環境変数: **0 件**(本フェーズ由来なし)
- 新規 AI 呼び出し: なし / 新規 LocalStorage キー: なし / 機能追加: なし

## 5. 全フェーズ累計

- フェーズ1〜8 で PR #399 まで到達(クラウド同期点火まで完了)。
- フェーズ9 は本作業を 14 コミット(+付随1)= 1 ドラフト PR に集約(運用注記参照)。
- 累計 PR は概ね **約 400 本**(#1〜#399 + 本 PR)。

## 6. 研ぎ澄まし完成度の最終判定

- **判定値: 9.5 / 10(9.5 割)**。フェーズ前 8〜9 割 → 削除5・統合3・磨き5 の完遂で到達。
- 二度手間(報告3重・模試3重・quickstart重複・注釈3層)を解消し、選択肢過多と視覚ノイズ
  (カード20枚・常時バッジ)を抑制。差別化の核(AI 解説・午後採点)を覆っていた摩擦を除去できた。
- 満点でない理由: ⑪⑫の体験は実機(モバイル実画面)での CLS/ファーストビューの最終確認が残る。

## 7. 残課題と将来対応の優先度

- **優先度 高**: ⑪(履歴ヒーロー差替)・⑫(FV整理)の**実機ビジュアル/CLS 検証**。本環境は
  ブラウザ非搭載のため画素・ハイドレーション挙動を未確認。デプロイプレビューで要確認。
- **優先度 中**: `/quickstart` 301 の実 301 応答・既存被リンクの遷移を本番で確認(設定はビルド検証済み)。
  `[exam]` ページ等に残る EducationalOrganization の重複(本タスク対象外)を将来整理可能。
- **優先度 低**: 「分野で探す」アコーディオンの折りたたみ既定が分野流入の指標に与える影響を計測し、
  必要なら既定開閉を再調整。

## 8. ipa-quiz-site クローズ判定

- **判定: 条件付きクローズ可(保守運用フェーズ移行を推奨)**。
- 機能網羅(フェーズ1-8)+ 研ぎ澄まし(フェーズ9)で、競合に対する差別化と体験密度は
  「完成」と呼べる水準。新規の大型機能は不要。
- クローズの**唯一の前提条件**: 本 PR を実機プレビューで ⑪⑫ のビジュアル確認後にマージすること。
  以降は問題データ追加・IPA 出典更新・軽微な保守のみで自走可能。
